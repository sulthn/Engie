// Handling Entity objects for OOP game instance code

// Inherited from instance.h/.cpp

#include <iostream>
#include <vector>
#include <string>

class Entity {
    public:
        Entity(std::string className) {}
    private:
};